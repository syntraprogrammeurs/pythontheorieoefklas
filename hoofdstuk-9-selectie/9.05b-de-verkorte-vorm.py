# Python basis — theoriebundel
# Hoofdstuk 9 — Selectie: if, elif, else en match
# 9.5 De verkorte vorm
#
# Gebruik hem alleen voor korte, eenvoudige keuzes, en nooit genest. Dit is technisch
# geldig en menselijk onleesbaar:
#
# Dit is een fragment uit de uitleg. Het gebruikt namen die elders gemaakt worden, en
# draait niet op zichzelf.

x = "a" if p else "b" if q else "c" if r else "d"
